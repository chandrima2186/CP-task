Question: Given a sorted array of distinct integers and a target value, return the index if the target is found. If not, return the index where it would be if it were inserted in order.
You must write an algorithm with O(log n) runtime complexity. (Leetcode)
Input: nums = [1,3,5,6], target = 5
Output: 2
Constraints:
1 <= nums.length <= 104
-104 <= nums[i] <= 104
nums contains distinct values sorted in ascending order.
-104 <= target <= 104 

#include <iostream>
using namespace std;
int f(int x[], int n, int m) {
    int l = 0, r = n - 1;
    while (l <= r) {
        int mid = l + (r - l) / 2;
        if (x[mid] == m) return mid;
        else if (x[mid] < m) l = mid + 1;
        else r = mid - 1;
    }
    return l;
}
int main() {
    int n, m;
    cin >> n;
    int x[n];
    for (int i = 0; i < n; i++) cin >> x[i];
    cin >> m;
    cout << f(x, n, m) << endl;
    return 0;
}
