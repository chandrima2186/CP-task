Question: Given a sorted array of integers and a target value X, de-termine whether X exists in the array. If it exists, return its index (0-based). If it does
not exist, return -1.
Input: First line contains n, the size of array. Second line contains n integers. Third line
contains integer X.
Output: Single integer: index of X or -1.


#include <iostream>
#include <vector>
using namespace std;
int binarySearch(const vector<int>& arr, int X) {
    int left = 0, right = arr.size() - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] == X)
            return mid;       
        else if (arr[mid] < X)
            left = mid + 1;  
        else
            right = mid - 1;  
    return -1; 
}
int main() {
    int n, X;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) cin >> arr[i];
    cin >> X;
    cout << binarySearch(arr, X) << endl;
    return 0;
}
